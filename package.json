{
  "name": "event-artillery",
  "private": true,
  "type": "module",
  "description": "Event Artillery monorepo — generate and send events to AWS EventBridge from any AsyncAPI spec",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "check-types": "turbo run check-types",
    "clean": "turbo run clean",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.11.0"
  },
  "packageManager": "pnpm@10.33.2",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "@vscode/vsce-sign"]
  }
}
